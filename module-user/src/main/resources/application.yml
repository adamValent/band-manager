spring:
  datasource:
    url: jdbc:postgresql://db-users:5433/users
    username: root
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true